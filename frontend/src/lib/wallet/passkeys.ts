/**
 * Passkeys unlock the wallet encryption password locally (Option A).
 * They do NOT sign blockchain transactions — EOAs + Safe + Pimlico remain unchanged.
 */

export const PASSKEYS_ENABLED = true
