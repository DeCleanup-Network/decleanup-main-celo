'use client'

import { SponsorSubmitPanel } from '@/components/sponsor/SponsorSubmitPanel'

export default function SponsorSubmitPage() {
  return (
    <main className="flex min-h-0 flex-1 flex-col bg-background">
      <SponsorSubmitPanel />
    </main>
  )
}
